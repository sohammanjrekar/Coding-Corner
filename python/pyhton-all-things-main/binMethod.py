# Define a list of strings
l = ["a", "b", "c"]

# Use the join() method to combine the elements of the list into a single string,
# with ", and ,  " as the separator between elements
result = ", and ,  ".join(l)

# Print the resulting string
print(result)
